server {
    listen 8080;
    server_name gigablast.wheee;

    root /site;
    index index.html;
    
    location / {
        try_files $uri /index.html;
    }
}

server {
    listen 8081;
    server_name gigablast.wheee;

    root /site;
    index index.html;
    
    location / {
        try_files $uri /index.html;
    }
}